# page.flash.notice "#{@asset.title} added to bucket."
page.insert_html :bottom, 'bucket', "<div>#{link_to image_tag(@asset.public_filename(:thumbnail)), @asset.public_filename(:normal)}\n
<div class='info' style='display:none'><p>#{@asset.public_filename(:normal)}</p>\n <p>#{@asset.public_filename(:thumbnail)}</p>\n <p>#{@asset.title}</p>\n </div></div>"